// Update these two values before taking real orders — see README.md for instructions.

// Formspree form ID. Sign up free at https://formspree.io, create a form,
// and paste the ID here (the part after "f/" in your form's endpoint URL).
export const FORMSPREE_FORM_ID = "your-form-id";

// Venmo handle shown on the order confirmation message, e.g. "@jane-smith".
export const VENMO_HANDLE = "@your-venmo-handle";
